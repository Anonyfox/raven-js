import assert from "node:assert";
import { describe, it } from "node:test";
import "./signature-phrase.js";

describe("SignaturePhraseProfile type (shape sanity)", () => {
	it("can be referenced without runtime exports", () => {
		assert.ok(true);
	});
});
