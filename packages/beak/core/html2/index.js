/**
 * @author Anonyfox <max@anonyfox.com>
 * @license MIT
 * @see https://github.com/Anonyfox/ravenjs
 * @see https://ravenjs.dev
 * @see https://anonyfox.com
 */

/**
 * @file HTML2 tagged template literal - clean slate for future optimization attempts
 *
 * This is a placeholder implementation that maintains the API signature
 * while providing a clean foundation for future performance-focused implementations.
 */

/**
 * Tagged template literal for HTML with context-aware escaping.
 *
 * @param {readonly string[]} _parts - Template literal static parts
 * @param {...any} _values - Template literal interpolated values
 * @returns {string} Empty string (placeholder implementation)
 */
export function html2(_parts, ..._values) {
	// Placeholder: return empty string
	return "";
}

/**
 * Tagged template literal for raw HTML without escaping.
 *
 * @param {readonly string[]} _parts - Template literal static parts
 * @param {...any} _values - Template literal interpolated values
 * @returns {string} Empty string (placeholder implementation)
 */
export function raw2(_parts, ..._values) {
	// Placeholder: return empty string
	return "";
}
